describe('testing the first test', () => {
  test(' 1 + 1 should equal 2', () => {
    expect(1 + 3).toBe(4);
  });

  test('Not equal 3', () => {
    expect(2 + 2).not.toBe(3);
  } )
});

/**
 * test() and it() are the same.
 */
